# Standard 9-output SCRFD template. The source ONNX is used to build a
# platform-specific engine on the first DeepStream run.
[property]
gpu-id=0
net-scale-factor=0.0078125
offsets=127.5;127.5;127.5
model-color-format=0
batch-size=1
network-mode=2
gie-unique-id=2
process-mode=2
network-type=0
num-detected-classes=1
operate-on-gie-id=1
operate-on-class-ids=0
infer-dims=3;640;640
onnx-file=/workspace/models/face-deepstream.onnx
model-engine-file=/workspace/models/face.engine
labelfile-path=/workspace/models/face.labels.txt
maintain-aspect-ratio=1
symmetric-padding=1
cluster-mode=2
custom-lib-path=/opt/nvidia/deepstream/deepstream/lib/libnvdsinfer_custom_scrfd.so
parse-bbox-func-name=NvDsInferParseCustomSCRFD
# Required for five-point landmark decoding on the parent person metadata.
output-tensor-meta=1

[class-attrs-all]
# CCTV recall is more important than rejecting every weak face proposal here.
# Downstream same-track quality selection and AdaFace verification decide which
# evidence is finally kept.
pre-cluster-threshold=0.35
topk=100
